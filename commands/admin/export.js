module.exports = {
  name: 'export',
  description: 'Exports data from the mysql server',
  aliases: [''],
  usage: ['export dataset'],
  reqMusic: false,
  execute(msg, args) {
    let type = args[0];
    let output = [];
    switch(type){
    }
  },
}
