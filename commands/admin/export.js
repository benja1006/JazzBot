module.exports = {
  name: 'export',
  description: 'Exports data from the mysql server',
  aliases: [''],
  usage: ['export dataset'],
  execute(msg, args) {
    let type = args[0].toLowerCase();
    let output = [];
    switch(type){
    }
  },
}
